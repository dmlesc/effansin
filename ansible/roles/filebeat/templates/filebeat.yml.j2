filebeat.prospectors:
- input_type: log
  paths:
    - /var/log/syslog*
  exclude_files: [".gz$"]
  multiline:
    pattern: "^\\s"
    match: after
output.logstash:
  hosts: ["{{ ansible_default_ipv4.address }}:5044"]
  index: syslog